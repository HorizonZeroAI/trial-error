# Exhibit: arrow of time — ML classifier on reversible dynamics

One experiment, exactly as the system recorded it, audit-verified. The
system built a molecular-dynamics gas with reversible microscopic
dynamics, computed coarse-grained entropy, and trained a classifier to
distinguish time-forward from time-reversed trajectories.

Result: entropy increase confirmed, classifier accuracy 1.000,
fluctuation test passed — an operational measurement of the arrow of
time emerging from reversible microdynamics.

I included this one on purpose because it's the sibling of a failed
experiment: the same research family produced a fluctuation-theorem run
that emitted zero output and got narrated as a success by the
evaluation layer anyway (see CLAIMS_AUDIT.md). Same family, one genuine
result, one silent failure. The audit tells them apart by artifact, not
by what the narrative said.
